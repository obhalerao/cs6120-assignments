extract = 'total_dyn_inst: (\d+)'
benchmarks = 'benchmarks/*/*.bril'

[runs.baseline]
pipeline = [
    "bril2json",
    "brili -p {args}",
]

[runs.tdce]
pipeline = [
    "bril2json",
    "./l3_tdce",
    "brili -p {args}",
]